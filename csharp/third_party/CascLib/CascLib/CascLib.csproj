<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <!-- D4Scanner (vendored): pinned to net8.0 to match the solution SDK; upstream multi-targets. -->
    <TargetFramework>net8.0</TargetFramework>
    <Copyright>Copyright © TOM_RUS 2014-2022</Copyright>
    <LangVersion>latest</LangVersion>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
    <Nullable>disable</Nullable>
    <PackageProjectUrl>https://github.com/WoW-Tools/CascLib</PackageProjectUrl>
    <RepositoryUrl>https://github.com/WoW-Tools/CascLib.git</RepositoryUrl>
    <Authors>TOM_RUS</Authors>
    <Version>1.0.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="MimeKitLite" Version="4.14.0" />
    <PackageReference Include="System.Runtime.CompilerServices.Unsafe" Version="6.1.2" Condition="'$(TargetFramework)' == 'netstandard2.0'" />
  </ItemGroup>

  <ItemGroup>
    <Compile Update="BackgroundWorkerEx.cs">
      <SubType>Component</SubType>
    </Compile>
  </ItemGroup>

</Project>
