# D4Scanner deep-research audit — 2026-06-13

Findings from a 5-lens parallel audit (TTS parsing · item/class/build modeling & scoring ·
UX/UI · latent bugs/reliability · game-mechanics ground-truth). Every item is grounded in
real code (file:line). Severity = user impact; Effort = S/M/L; Conf = confidence the finding
is real. ⚑ = needs an in-game capture to confirm the tooltip shape before fixing.

**Overall health: good.** Of the gearing-doc §8 gap table (17 items), ~14 are already closed.
The remaining issues are concentrated in (a) a few sharp correctness bugs, (b) crash-time
data-safety, (c) discoverability/empty-state UX, and (d) class/build coverage breadth.

## Continuous-improvement finds (post-audit fresh reviews)
- [x] **CI-15 / D6 (partial) · LoH talisman charms of all rarities were silently dropped.** _(v0.71.0 — UNBLOCKED
  by the user's input ("all builds use talismans"): mining their real Talisman-screen log revealed the LoH charm
  format — "Rare Charm" / "Magic Charm" / "Charm (Ancestral)" — but the parser only had literal "Set Charm"/
  "Unique Charm" type keys, so a Rare charm matched no type → DetectRarityType false → no slot/rarity →
  LooksLikeItem failed → DROPPED. An entire gear category never appeared. Added `ReCharmType` (anchored
  "<Rarity>? Charm (Ancestral)?") as a DetectRarityType fallback — won't false-match the seal's "Unlocks N Charm
  Slots" or all-caps charm NAMES, won't flip a seal to a charm. Seals already parse via "Horadric Seal". +8
  assertions on verbatim log lines. Captured charms now show in All Items; doll talisman section + set-bonus
  tracking + the seal-capture-flow question remain (D6 continues). The log ALSO confirmed the seal name is
  "Horadric Seal" (not the DB's internal "BloodSeal"), and Maxroll still exports no talisman TARGETS.)_
- [~] **CI-14 / D7 (partial) · Loot-filter export enriched toward the native filter's conditions.** _(v0.70.0 —
  first of the audit's ⚑ items advanced (not blocked): the doc §6 lists the native loot filter's rule conditions
  (IP range, Ancestral, required affixes…), so the markdown export became a filter-SETUP guide. Added the
  Ancestral/Item-Power floor (the #1 endgame rule, was absent) + a cross-slot "Priority affixes" summary (ranks
  affixes by how many slots want them — a higher-value filter condition). Markdown-only (always-useful artifact);
  the D4Companion preset's exact IP/GA/codex-upgrade schema + the native FILE format remain the best-effort/
  blocked parts. +3 assertions. Done after autonomous bug-hunting genuinely converged (4 clean iterations, every
  code surface incl. all parsers reviewed) — pivoted to enhancement per the user's "keep going".)_
- [x] **CI-13 · Missing Torment-12 gate left near-endgame players with no progression push.** _(v0.69.0 — fresh
  modality: cross-check encoded SeasonPack DATA vs the game-mechanics doc ground truth. `tormentGates` topped out
  at T11; Activities' tier-gating (`FirstOrDefault(g => g.Tier > t)`) therefore returned null for a T11 player
  (build incomplete) → no "push to next tier" rec, though T12 is the doc's "best rates / gear-farm" tier. The
  user's Rogue is at T10, so this is their literal next step. Added the T12 gate + made the "higher tiers also…"
  suffix conditional (T12 is the top). +3 assertions. **Rest of SeasonPack validated CLEAN vs the doc**: socket
  capacity, masterwork constants (Quality 25 / floor(3.75·Q+10) / ×2 2H), obol prices + cap 2500, Hordes spoils
  + Aether (400/666), boss ladder, IP floor 900, glyph constants — all match. The 8-class roster, 850/900 IP,
  Torment I–XII, 1-temper-slot, uniques-temperable are all correctly reflected.)_
- [x] **CI-12 · Imported builds leaked unmapped-id garbage aspect names.** _(v0.68.0 — validating the real
  Whirlwind Barbarian build diff: a junk aspect "BSK Barbarian 001 2" appeared in the Aspects list (0/8) and as
  an actionable "IMPRINT BSK Barbarian 001 2" step. Root cause: a legendary-aspect key whose name never resolved
  → `AspectName` fell through to the humanized raw id. The importer's `LooksLikeItemId` guard (already applied to
  uniques) only matched RARITY-word ids + digit, missing CLASS-word ids ("Barbarian"), and wasn't applied to
  aspects at all. Fix: broaden `LooksLikeItemId` to class words (made public), apply it at the aspect-import
  site, and heal already-saved builds in `TargetLoader.Load` (strip garbage from Aspects + per-slot Aspect, no
  re-import). Verified on the real build: 0/8 → 0/7, junk gone from list + guidance, 7 real aspects intact. +10
  assertions. Found by the Barbarian-class diff — the angle I almost skipped, vindicating not over-claiming
  convergence after the Rogue-only validation.)_
- [x] **CI-11 · Loot-filter export silently dropped slot-less aspects.** _(v0.67.0 — validating the LootFilter
  export (un-exercised feature) against the real Dance of Knives build: it has 8 aspects in `TargetBuild.Aspects`
  but only 7 pinned to a gear piece's `TargetGear.Aspect`. Both `Markdown` and `CompanionPreset` derived aspects
  ONLY from per-gear Aspect → the 8th loose aspect ("Aspect of Accursed Touch") was silently dropped from the
  exported filter, though the diff tracks it (DO NEXT lists it). Added `LooseAspects` + an "## Other Aspects"
  markdown section + slot-less companion-preset entries. +5 assertions. Also informs ⚑ D7 — confirms the
  D4Companion Type/Id mapping is the remaining best-effort gap (needs their schema).)_
- [x] **CI-10 · Pure capture-artifact items leaked into All Items.** _(v0.66.0 — full-log real-data validation:
  scanning all 136 persisted items across the user's profiles + live.json found a phantom "Ring" (RawName
  "RING", IP 850, null slot/type/rarity, 0 affixes, stray "118 All Resist" PowerText). Traced to a Talisman-panel
  log sequence (EQUIPPED/RING/Ring/850 Item Power/118 All Resist) under a PRE-FIX parser; the current parser
  consumes "RING"/"Ring" as slot headers and produces nothing (verified by hand-trace), but the stale artifact
  persists until re-hover (the known pre-fix-junk class) and shows as a junk row in All Items. Added
  `GearList.IsCaptureArtifact` (no slot AND no type AND no rarity AND no affixes) and filtered it from `Build()`
  + `SharedCandidates()`. Display-only (those feed only MainWindow:3030, never scoring/diff); predicate is
  maximally conservative (one of slot/type/rarity/affix ⇒ kept). +3 assertions. The other scanned classes were
  clean: 0 rares/uniques with 0 affixes; the 18 "Weapon Damage" PowerText lines are benign (correctly dropped as
  affixes by ReWeaponStat, kept as display-only PowerText, never target-matched).)_
- [x] **CI-9 · Skill rows mislabeled rank as "pts" and discarded the game's base-max.** _(v0.65.0 — surfaced by a
  NEW validation modality: real-data functional validation (CLI diff of the user's Rogue build vs captured gear).
  Skill rows read "27 pts" — mislabeling a RANK as points AND throwing away the "/Y" base-max the game voices in
  "RANK X/Y" (X−Y = the +Ranks gear/paragon bonus, build-relevant). Added `LiveSkill.BaseMax` (SkillParser group
  2); DiffEngine now renders "rank X/Y" with a "rank X" fallback for older-parser live.json. +2 assertions.
  **Two other anomalies that pass surfaced were verified NON-bugs:** (a) skill ranks 27/28 — raw TTS confirms the
  game voiced "RANK 27/15", real effective ranks, parser faithful; (b) "Sea Lord's Fine Gloves" in the build's
  Uniques — hand-edited build data (`.pre-uniques.bak` shows it was added post-import; no `.bak`-writing code
  exists), code handles it correctly. Functional validation = a 4th modality (after code review, visual QA) that
  caught what static review couldn't, validating the user's instinct to keep iterating.)_
- [x] **CI-8 · GearParser used throwing int.Parse for 4 numeric fields (convention break + season-volatile risk).**
  _(v0.64.0 — adversarial read of the parser: Quality, Masterwork rank/max, Temper used/max, and Requires-Level
  used `int.Parse` on unbounded `\d+` captures, while ItemPower/Dps/SocketCount/SetActive/Total all use the
  null-safe `(int?)ToNum`. On a malformed/oversized digit run the int.Parse sites throw OverflowException and
  fault the WHOLE block parse, while the rest degrades gracefully. Not reachable on today's exact TTS text, but
  CLAUDE.md flags TTS format as the #1 season-volatile bug source — a future digit-glue would crash 4 fields
  while the others tolerate it. Unified all four on `(int?)ToNum`, matching the parser's own convention. +6
  assertions (oversized doesn't throw, affix still parses past it, ordinary values unchanged). The rest of
  GearParser reviewed clean on realistic input: ParseAffix/RecoverMidStringAffix ToNum-guarded, segmenter
  slot-header expiry + stale-block discard correct, GA inference matches doc design.)_
- [x] **CI-7 · OCR scan path leaked two more WinRT objects per scan (CI-6 cluster).** _(v0.63.0 — the cluster
  hypothesis from CI-6 ("finds cluster — the sibling capture file likely shares the leak") confirmed: reviewing
  `OcrCaptureEngine` end-to-end found (a) `ScanCoreAsync`'s `sb` — the SoftwareBitmap from
  `BitmapToSoftwareBitmapAsync`, consumed by `RecognizeAsync` then never disposed, leaked a full-screen native
  bitmap (~33 MB at 4K) to the finalizer on every changed-frame scan (~every 20 s during play); and (b) the
  `DataWriter` inside `BitmapToSoftwareBitmapAsync`, created/used/never disposed. Fixed `sb` with `using`; fixed
  the writer with `DetachStream()` + `Dispose()` (detach first so disposal doesn't close `iras`, still needed by
  the decoder). Both headlessly-reasoned-correct — no GPU/device lifetime involved (unlike CI-6's ⚑ d3dDevice).
  Rest of the file clean: skip-if-busy `Interlocked` guard, FrameHash LockBits/UnlockBits, TrySavePortrait
  `using` clones, OcrEngine (not IDisposable).)_
- [x] **CI-6 · WGC frame-grab leaked a native SoftwareBitmap per OCR scan.** _(v0.62.0 — a resource-disposal
  sweep of the native capture path found `GrabViaWgcAsync` created the intermediate `SoftwareBitmap` via
  `CreateCopyFromSurfaceAsync` (a copy we own) but never disposed it; the helper only disposes its internal
  `converted` copy. Every successful Windows.Graphics.Capture grab leaked a full-screen-sized native bitmap
  (~33 MB at 4K BGRA8) to the GC finalizer, accumulating native-memory pressure between GC passes during
  periodic OCR scanning. Marked `sb` as `using` — it's consumed synchronously into an independent
  System.Drawing.Bitmap, so disposal at method exit is safe and the result is unaffected. App-side
  native-interop code (no headless test surface). Found by sweeping the one disposal-sensitive area the
  Core-focused audit + prior sweeps hadn't covered.)_
  - ⚑ **Adjacent (needs live capture to validate):** `GrabViaWgcAsync` also never disposes the per-grab
    `IDirect3DDevice` (`d3dDevice`, line 94) — another create-per-grab WinRT/GPU resource left to the
    finalizer. Disposing it (reverse-order, after the pool/session and the awaited surface copy) is *probably*
    safe, but a D3D device-lifetime mistake silently degrades WGC→PrintWindow via the caller's catch, which
    can't be confirmed headlessly. Left for a session with live in-game capture.
- [x] **CI-5 · Non-atomic writes of settings + build files (B1 class, missed).** _(v0.61.0 — a WriteAll*-sweep
  found `app.json` (SaveSettings) and the imported `builds/<name>.json` were written non-atomically — a crash
  mid-write corrupts settings (log path, toggles, retention, window geometry) or the loaded build. Both now
  temp-file + Move, matching B1's live.json/profiles. (def-cache, maxroll-cache, tombstones, profiles were
  already atomic; build_index.json + icon PNGs are re-fetchable caches — left as-is.))_
- [x] **CI-4 · `ProfileStore.All()` surfaced a phantom blank profile.** _(v0.60.0 — `tombstones.json` lives
  in the profiles dir and deserializes to a CharacterProfile with empty slug/name; `All()` only filtered
  active.json + nulls, so it returned a PHANTOM blank character (would show in the switcher) and kept
  `All().Count > 0` (blocking legacy migration). `All()` now skips empty-slug/name entries — matching the
  guard `ResetAllLive` already had. Found by inspecting the user's real profiles dir; +1 test.)_
- [x] **CI-3 · `LogStore.ReadSession` could truncate a large session replay.** _(v0.59.0 — used
  `fs.Read(buf,0,len)`, which may return a PARTIAL read for a multi-MB session, so "load build from a past
  session" / replay could silently drop the tail (missing gear). Switched to `fs.ReadExactly`. Existing
  ReadSession test confirms no regression; same partial-read class as B5.)_
- [x] **CI-2 · Updater truncated hyphenated release tags.** _(v0.58.0 — `FindStagedUpdate`/`CleanUpSuperseded`
  parsed the version via `Split('-')[1]`, truncating "v1.0.0-rc1"→"v1.0.0" (mis-labels the update, can
  rename/clean the wrong file); `IsNewer` rejected any suffixed tag outright. Added `TagFromAssetFile`
  (full-tag extraction) + numeric-core comparison in `IsNewer`. Latent today (tags are clean vX.Y.Z) but a
  foot-gun the moment a pre-release tag ships. +6 tests. (Was bug-agent #6, uncaptured in the original tiers.))_
- [x] **CI-1 · DO NEXT flooded with one EQUIP step per owned upgrade.** _(v0.57.0 — a single slot with
  several owned upgrades (e.g. four boots) emitted four "EQUIP Boots" steps. `BuildGuide` now emits ONE per
  slot — the best by build-affixes-met — and notes "(+N more owned)". Found by rendering the real Rogue
  overview; +1 test.)_

### Verified-clean sweeps (no ship — coverage record)
- **Last untried surfaces — build/release config + TTS shim source.** `release.yml` sound (shim rebuild has a
  correct git-checkout fallback; tag→version injection is intended; `fail_on_unmatched_files` guards publish;
  14 successful releases confirm it — the only nit is the dormant optional-signing `if: env...` GHA edge, not
  touched against a working pipeline). `saapi64.cpp` clean (22-char ISO prefix exactly matches the parser's
  `s[21]==']'`; UTF-8 conversion correct; path-once + dedup documented; vendored/prebuilt). **D6 Talisman
  CONFIRMED blocked** — neither real build carries any seal/charm/talisman/set target data, so target-side
  Talisman can't be de-risked like D2 was; implementing blind would be speculative. **Every app-behavior code
  surface is now reviewed; every ⚑ is blocked or done — definitive convergence.** All clean and robustly bounded: `CharSelectParser`
  (detail-block state machine; RomanToInt validates+clamps 1–12; `int.Parse` bounded `\d{1,4}`; DEFINITIVELY
  resolves the Warlock profile — a block completes only via Paragon OR Level, the Warlock has Paragon=None so
  it's a real low-LEVEL Warlock, class correctly captured), `RosterParser`/`CharacterResolver` (bounded parse;
  resolvers null/Ambiguous on any collision — never silently mis-bind; nameplates skipped from identity),
  `CharacterParser` (TryParse + range guards `<2000`/`<100000` everywhere), `AffixAggregate` (progress math
  clamped, sound fallback), `AffixCeilings` (straightforward max-harvest). No ship — every parser is sound.
- **App settings draft/apply state machine.** `ApplySettings` is atomic and sound: staged log-move aborts
  cleanly (game-running / dest-exists / move-failure all return with the draft intact), TTS-uninstall confirm
  aborts the whole save on decline, exactly one SaveSettings + one StartWatching at the tail. Cache-clear is
  immediate via the card's own button (ApplySettings deliberately ignores CacheChecked) — self-consistent, a
  reasonable divergence from the plan's queue design, not a bug. No ship.
- **UpgradeScorer logic vs the doc §7 decision tree.** Sound: slot/type gating (§7.2), presence-beats-absence
  (§7.3), one-enchant-fixable credit (§7.4, with the `Count>1` conservative guard), GA-on-wanted capped (§7.5),
  roll-quality tiebreak (§7.6), endgame IP-tier break (§7.1). The tempered-line-not-enchantable nuance (§7.4) is
  the same un-fixable per-affix-attribution limit already documented for GAs. `GoalScore` is computed + tested
  but unused in the sort and unread by the App — vestigial/harmless; removing or wiring it both risk regressing
  the tuned per-slot ordering for zero user benefit, so deliberately left. No ship.
- **Stale-term tripwire scan vs the doc's FULL list (guidance ×12 tiers + serialized SeasonPack).** Expanded the
  tripwire test to sweep guidance output across all 12 Torment tiers AND the full serialized season pack (not just
  one sample build's surfaced text), checking the doc §1 tripwire list (Living Steel, Torment IV, "can't be
  tempered", 4/8/12 masterwork crits, 2-temper-slots, 6-class roster, 925-IP, …). All clean — the season data
  carries no pre-2026 mechanics. Test-only (no behavior change, no release); hardens the #1 churn source ahead of
  the S14 re-verification (~June 30) and realizes the plan's "tripwire list AS the test" intent. +11 assertions. After CI-12,
  scanned all 151 name fields across both builds (affixes/uniques/aspects/skills/key-passives) for other
  unmapped-id garbage — only the already-fixed "BSK Barbarian 001 2" flagged; importer name resolution is
  otherwise clean. False-negative check (log item-hovers vs parsed loadout) was inconclusive — the 299 gaps are
  legitimate historical vendor/stash/drop churn, not parse misses (parsed set = current 57-item loadout). Log is
  clean UTF-8 (0× Win-1252 apostrophes; U+2019 correctly E2 80 99); the `�` seen in a python dump was a
  Windows-console display artifact, not a decode bug (current apostrophe items parse correctly). No ship.
- **Verdicts validation (real Rogue inventory, 51 items).** Logic sound: the KEEP-DUPE copies-count excludes the
  equipped copy by design (verdict answers "what about this spare?"; flagging only at 2+ spares is conservative,
  not a bug). GA-count = 0 across all 51 spares is LEGITIMATE (values within range, e.g. 3740/4400 = 0.85× <
  1.2× GA threshold; the GA items are equipped). Barbarian profile (16 items) clean — no null slots/types, no
  0-affix rares; the Dagger in Barb bags is cross-class loot, correctly CanEquip-filtered. ⚑ **Real limitation
  (data-blocked):** dropped legendaries store the aspect EFFECT in PowerText but no aspect NAME (TTS only voices
  the name for imprinted items), so KEEP-SALVAGE can't flag build-relevant dropped legendaries — needs an aspect
  effect→name DB, not safe-fixable blind. Adds to the ⚑ data-blocked set.
- **Disposal/lifetime sweep, App outside `Capture/` (after CI-7).** Verified clean: `_watcher`/`_captureEngine`/
  `_updateTimer`/`_targetPoll` all use dispose-before-reassign in `StartWatching` AND dispose on `Closed`; both
  local `DispatcherTimer`s (toast ~1870, update-spinner ~2155) self-`Stop()`; all `FileStream`/`MemoryStream`
  are `using`; `App.xaml.cs`'s single-instance `Mutex` is intentionally process-lifetime.
- **Process-handle non-finding.** The hot-path `Process.GetProcessesByName(...).FirstOrDefault()` (WGC:59,
  OcrCaptureEngine:48) and the `.Length > 0` game-running checks (MainWindow 782/2676, CaptureSetup 205/260)
  read only `.MainWindowHandle`/`.Id`/`.Length` — none of which opens a kernel handle (snapshots are
  lightweight; a handle opens lazily only on `.Handle`/`.HasExited`/`.MainModule`/`.WaitForExit`). Disposing
  them releases nothing, so adding `using` would be churn — deliberately NOT done.
- **Net:** the disposal vein is drained — it yielded the two real bitmap/writer leaks (CI-6, CI-7) and then
  went clean.
- **Logic-file sweep (highest-impact + least-traveled).** Reviewed clean on realistic input: `LiveGearResolver`
  (Merge/MergeInventory Tts-precedence, DedupeInventory two-pass collapse, SanitizeEquipped — verified the
  `kept.Remove` inside `foreach(GroupBy(kept))` is safe because LINQ GroupBy eagerly buffers its source before
  yielding); `GearList` (Fingerprint content-canon, SharedCandidates' fingerprint→strict-domination collapse with
  correct ReferenceEquals self-guard); `UpgradePath` (crafting-order logic sound; only the quarterstaff `IsTwoHand`
  cost-estimate nuance is class-data-dependent — ⚑).
- **`heoki-warlock` profile — verified NON-bug.** Looked like a phantom (Name HEOKI, Class Warlock, 0 gear/0 inv,
  Paragon None), but the gearing doc confirms Warlock is a REAL S13/LoH class (roster = 8) and line 12 notes the
  user's own data includes a Warlock character. It's a real ungeared character (class set from char-select voice,
  which precedes gear capture). Filtering it would HIDE a real character — deliberately NOT done. Paladin/Warlock
  conservative class handling is documented (§8 #15, ⚑ pending weapon tables).
- **Visual render QA (modality code review can't cover).** Headless-rendered 6 states with the real Rogue build +
  live.json — overview (1440 wide + 860 narrow reflow), compare, All Items, settings, target. All clean against
  the never-clip / always-wrap rules: header stacks correctly at narrow width, DO NEXT reflows below the doll,
  all modals fit, text wraps. No clipping, no letter-wrap.
- **Convergence (both modalities):** 8 genuine fixes shipped (CI-1…CI-8); every high-impact + least-traveled logic
  file reviewed clean; a bug-shaped real-data anomaly resolved to correct behavior; 6 UI states visually verified.
  Remaining high-value work is blocked on user input: ⚑ D6 Talisman target source, ⚑ D7 native loot-filter export
  format, ⚑ D8 per-target Torment-tier data, ⚑ d3dDevice live-capture lifetime, ⚑ Paladin/Warlock weapon tables.

---

## Tier A — Correctness bugs (the app shows the user wrong information)

- [x] **A1 · Mythic uniques can be verdicted "Junk".** _(v0.45.0)_ `GearParser` only sets `IsAncestral`
  when the literal word "Ancestral" is voiced; a Mythic tooltip without it leaves
  `IsAncestral=false`, and `Verdicts.For` (`Verdicts.cs:76`) then junks any non-ancestral
  item under IP 900. A Mythic is never junk. **Fix:** `IsMythic ⇒ IsAncestral=true` in
  `GearParser` (`:166/170`); also guard the floor rule with `&& !IsMythic && !IsUnique`.
  *Sev High · Eff S · Conf High*
- [x] **A2 · Residual vendor-gear-leak hole** _(v0.45.0 — positive Character panel now required)_ (found independently by 2 agents). A vendor/bag
  hover can still be stamped *worn* when `_currentPanel` is stale-"Character" (a missed
  Vendor/Purveyor marker) and the block closes with no action tail: `ClassifyContext`
  (`LogWatcher.cs:411`) trusts `UiPanel=="Character"`, and `GearParser.SlotHeaders` still
  contains vendor-ambiguous words (Ring/Neck/Ranged) that arm `FromCharPanel` without setting
  the panel. **Fix:** expire `_currentPanel` after N quiet lines, and require a corroborating
  Character signal (not bare `null`) in the no-action-tail branch. *Sev High · Eff M · Conf High*
- [x] **A3 · Skill-rank affixes mis-parse** _(v0.45.0)_ — the #2 highest-leverage affix in the game.
  `+3 Ranks to Core Skill` parses to affix text `"Ranks to Core Skill"` because
  `ReLeadingConnective` (`GearParser.cs:78`) only strips a *leading* "to ". `PhraseMatch`
  then under-matches build targets ("Core Skills" plural, umbrella "All Skills").
  **Fix:** strip a leading `Ranks?\s+to\s+`; add a `+N Ranks to X` fixture assertion.
  *Sev High · Eff S · Conf High*
- [x] **A4 · The "under-rolled / POLISH" guidance tier is effectively DEAD for imported builds.** _(WON'T-CHANGE — this is the DELIBERATE v0.42 tradeoff: the roll-quality gate was removed for presence-based scoring, and v0.42's own release notes named this exact fallout. Re-adding thresholds would reverse that decision. Working as designed.)_
  `MaxrollImporter.ParseItemAffixes` never sets `Min`/`MinPercent`, so `EvalSlot` always
  scores by presence and Status "under" can't fire — which silences `BuildGuide` IMPROVE/
  RE-TEMPER steps and the masterwork/GA `Activities` recs for every real import. **Fix:**
  decide intent — either derive an implicit threshold (harvest target maxima) on import, or
  remove the dead branches and document it. *Sev High · Eff M · Conf High*
- [x] **A5 · Aspect double-counting inflates completion %.** _(v0.47.0 — VERIFIED the gear-vs-Aspects "double count" was a misread: the per-slot aspect is display-only WantAspect, never in a group's Total; aspects count once, in the Aspects category. Fixed the one REAL case: deduped the global list so the same aspect on two slots isn't counted twice.)_ `MaxrollImporter` pushes each
  slot's imprinted aspect to BOTH the slot (`WantAspect`) and the global Aspects category
  (`:364`), which `Diff` re-checks independently — so a 6-aspect build adds 6 duplicate rows
  to `Total`. **Fix:** don't add slot-imprinted aspects to the global Aspects list.
  *Sev Med-High · Eff M · Conf High*
- [x] **A6 · A badly-rolled unique shows 100% met.** _(v0.48.0 — VALIDATED against the real Dance-of-Knives build + live.json: owned Shrouded Gift → 4/5 (missing Armor), Sea Lord's → 1/2 (missing Core Skills). Shipped as an ADVISORY `RollNote`: presence still counts the unique as met (v0.42 model, headline % unchanged), but a badly-rolled owned copy is now flagged to prompt chasing a better one. The counted variant was REJECTED — it balloons the Uniques total 5→~22 and double-penalizes UNOWNED uniques' secondaries, distorting the %.)_ `TargetUnique.Affixes` is imported but
  the Uniques diff (`DiffEngine.cs:571-584`) only checks presence by name; it never scores
  the owned copy's rolled secondaries (gearing doc §4 says treat uniques as rollable).
  **Fix:** run owned-unique secondaries through `EvalSlot` and surface under-rolled sub-rows.
  *Sev Med-High · Eff M-L · Conf High*
- [x] **A7 · GA count silently null when the temper line is missing/truncated.** _(v0.47.0 — Mythics now floor to all-GA from rarity; the per-affix/temper inference is unchanged for non-Mythics.)_ GA count is
  derived only from `TemperMax-3`; a poll-edge-truncated block or a tooltip without the temper
  line drops it to null and the item is graded GA-unknown. **Fix:** floor GA from rarity
  (Mythic ⇒ all GA); mark "GA unknown" distinctly from "0 GA". *Sev Med · Eff M · Conf Med*

## Tier B — Reliability / crash-time data safety

- [x] **B1 · Non-atomic writes corrupt data on crash.** _(v0.45.0 — live.json + profiles + active.json)_ `live.json` (`MainWindow:1085`) and
  `ProfileStore.Save` (`:62`) use plain `File.WriteAllText`; a kill mid-write truncates the
  file, which then silently deserializes to null/empty — the character's loadout, bound build,
  paragon/Torment vanish with no error. (Tombstones/icons already write atomically — match
  them.) **Fix:** temp-file + `File.Move(overwrite)`. *Sev High · Eff S · Conf High*
- [x] **B2 · Silent ~70 MB update download on every launch.** _(v0.45.0 — in-flight guard; auto-download-vs-prompt left as a product choice)_ `CheckForUpdatesAsync`
  (`MainWindow:1852`) auto-`DownloadUpdateAsync` (~70 MB) before the user sees any "update
  ready" UI, re-fires on every `StartWatching()` (each setting change / `SessionEnded`), and
  has no in-flight guard (two writers can race the staged file). **Fix:** prompt-then-download
  or gate behind a setting; add a "checked/​downloading this run" guard. *Sev High · Eff S · Conf High*
- [x] **B3 · OCR scan has no re-entrancy guard.** _(v0.45.0)_ `OcrCaptureEngine.ScanCoreAsync` mutates
  `_orderedGear/_orderedInv/_lastHash` with no lock; a >20 s scan lets the next timer tick
  start concurrently → torn lists / `IndexOutOfRangeException`. (`LogWatcher.Poll` already
  guards with `Interlocked`.) **Fix:** add the same skip-if-busy guard. *Sev Med-High · Eff S · Conf High*
- [x] **B4 · Negative-cache latches until restart.** _(v0.45.0 — no-negative-cache + atomic/self-healing caches)_ `GameDataIcons.GetDef` (`:251`) caches a
  `null` def on a single fetch failure → that atlas stays silhouettes for the whole process;
  `MaxrollImporter.LoadJsonCached` (`:441`) caches a truncated download (non-atomic write,
  `File.Exists` skip) → every future import/icon resolve throws until manual clear. **Fix:**
  don't cache negatives; atomic write + delete-on-parse-failure. *Sev Med-High · Eff S · Conf High*
- [x] **B5 · UTF-8 multibyte split across poll chunks** _(v0.51.0 — `LogWatcher.Poll` now streams UTF-8 via a persistent `Decoder` (reset on rotation); extracted a public `FeedBytes` seam mirroring `FeedChunk`, with a test that splits an accented name mid-character and fails under the old per-chunk decode.)_ — original: → mojibake in an accented item/char
  name, which then mis-keys dedup/tombstone/profile matching. `LogWatcher.Poll` keeps a partial
  *line* but decodes each byte chunk independently (`:160`). **Fix:** keep undecoded trailing
  bytes / use a streaming `Decoder`. *Sev Med · Eff M · Conf Med* ⚑(only bites non-ASCII names)
- [x] **B6 · "Adopt stray log" splices raw bytes into the live tail** _(v0.53.0 — now parses the stray OFFLINE via BuildFromLines and merges through OnLiveUpdate, so an embedded attach marker can't wipe the live loadout and there's no splice-seam mis-parse.)_ — original: (`MainWindow:667`) — if
  the stray contains an attach marker it wipes accumulated gear; otherwise the seam mis-parses.
  **Fix:** feed the stray through `BuildFromLines` offline and merge. *Sev Med · Eff M · Conf Med*
- [x] **B7 · Tombstone resurrection on replay (inventory path).** _(v0.45.0)_ During `_replayFromZero` the
  gear path skips `ObserveSightings`, but `_tombstones.Apply(inv)` (`MainWindow:603`) observes
  sightings unconditionally → deleted inventory items reappear after a live-gear clear. **Fix:**
  filter-only `Apply` (no observe) during replay. *Sev Med · Eff S · Conf Med*

## Tier C — UX / discoverability

- [x] **C1 · Silent empty doll = the #1 "is it broken?" moment.** _(v0.46.0 — EmptyGearBanner: set-up vs. nothing-seen, with Diagnose/Settings action)_ Once a build is loaded the
  capture-setup entry point disappears (it only lives in the no-build WelcomeCard); a configured
  user who sees zero gear gets all-empty slots with no banner, no reason, and no "open D4's
  character panel and hover" hint (that hint is 3 clicks deep in Diagnose). **Fix:** when a build
  is loaded but capture is unconfigured or 0 gear is seen, show an inline banner + a Diagnose
  link. *Sev High · Eff S-M · Conf High*
- [x] **C2 · Esc doesn't close the All-Items or Update modals** _(v0.45.0)_ (they live in `RootLayer`, not
  the Esc-aware `HelpHost`/`SettingsHost`) — breaks the contract every other overlay follows.
  **Fix:** handle Esc for the topmost `RootLayer` overlay. *Sev Med-High · Eff S · Conf High*
- [x] **C3 · Status bar clips with `TextTrimming.CharacterEllipsis`** _(v0.45.0 — wrap + tooltip)_ (`MainWindow.xaml:147`) —
  the one literal violation of the "never clip text" rule; it hides live status and import/export
  errors at narrow widths. **Fix:** drop trimming; shrink/relocate `StatusDetail`. *Sev Med-High · Eff S · Conf High*
- [x] **C4 · Pin / Compare / Focus / Select are an undiscoverable, overloaded triad** _(v0.46.0 — title-bar "Pin"→"On top" + tooltip frees the term; interactions now documented in Help, see C7)_ — clicking a
  slot silently pins it; "Pin" also names the always-on-top button. No in-UI explanation. **Fix:**
  add an affordance hint near the doll, reconcile terminology, rename the title-bar "Pin"→"On top".
  *Sev Med · Eff M · Conf Med*
- [x] **C5 · Text links have no hover affordance** _(v0.46.0 — MakeLink underlines on hover)_ — `MakeLink` is a bare `TextBlock`; "Export",
  "← Overview", "Build spec", "focus", etc. don't read as clickable. **Fix:** give `MakeLink` a
  built-in hover (underline/brighten). One method, broad payoff. *Sev Med · Eff S · Conf High*
- [~] **C6 · No progress for long ops** — _(v0.46.0: import already pops a MessageBox on failure — that half is fine. The icon-extraction "loading icons…" indicator needs GameDataIcons queue-state plumbing → moved to Phase 4.)_ first-run icon extraction shows a wall of silhouettes
  for seconds-to-minutes with no "loading icons…" signal; import success/failure feedback is
  inconsistent (status text vs Toast vs MessageBox). **Fix:** status-bar note while the icon queue
  drains; standardize op feedback. *Sev Med · Eff M · Conf High*
- [x] **C7 · Help overlay omits the mouse interactions it most needs to teach** _(v0.46.0 — added an INTERACTIONS section)_ (only lists
  keyboard accelerators). **Fix:** add an "Interactions" section (click=pin&compare, hover=peek,
  badge legend, the 3 doll tabs). *Sev Low-Med · Eff S · Conf High*
- [x] **C8 · Header doesn't reflow at narrow widths** _(v0.54.0 — below ~900px the action buttons stack onto their own wrapping row beneath the now-full-width search box; render-verified both that the normal-width header is unchanged AND the narrow layout reflows. Uses a header-specific threshold, not the doll's TwoColMin.)_ — remaining sub-point: — 5-6 buttons + search box can squeeze the
  search to a sliver; the `OverallCount` line wraps and desyncs the big % baseline. **Fix:**
  overflow/icon-collapse secondary buttons below `TwoColMin`; move "⚠ N below min" off the count
  line. *Sev Low-Med · Eff M · Conf Med*
- [x] **C9 · `Faint` (#64615A) micro-text is below WCAG contrast** _(v0.49.0 — bumped Faint #64615A→#85827B; render-verified the cool-dark aesthetic holds while informational micro-text (slot labels, legend, hints) reads comfortably. Still clearly below Soft, so the secondary-text tiering is preserved.)_ and is used for real content
  (slot labels, max targets, legend). **Fix:** reserve `Faint` for disabled; bump info text to
  `Soft`. *Sev Low · Eff S · Conf Med*
- [x] **C10 · Functional emoji** _(v0.55.0 — the color emoji clashed with the cool-dark monochrome palette: 🗑→✕, 📌→＋ (matches the app's "＋ Open" style), 👁→◉ (monochrome lens, matches the TTS card's "⌨"). ⚒ left as-is (already a monochrome dingbat). Render-verified ✕/＋ in All-Items.)_
- [x] **C11 · Empty weapon placeholders only render for Barbarian/Rogue** _(WON'T FIX — assessed: the Barb/Rogue gate is deliberate. Caster weapon counts are ambiguous (2H staff = 1 vs wand+focus = 2), so padding to a fixed 2 would show a phantom slot; and target weapon sections already populate the weapon area for loaded builds.)_ — other classes with 0
  captured weapons show no weapon area at all. **Fix:** pad to `ClassRules.WeaponSlots` for all
  classes. *Sev Low · Eff S · Conf High*

## Tier D — Item / class / build coverage

- [⚑] **D1 · Class weapon rules are incomplete → valid items get demoted.** _(CONFIRMED BLOCKED — the gearing doc itself (§8 #15) says the new-class weapon tables are UNVERIFIED and to "keep conservative bypass," and I can't verify Spiritborn's full weapon set from the user's Rogue/Barbarian data (whose tables are already correct, so no false demotions there). Needs an authoritative class×weapon matrix; a blind fail-open could let genuinely-wrong-class items through. Unlike D2, not de-riskable from on-disk data.)_ `ClassRules.WeaponUse`
  is a positive allow-list that omits Spiritborn/Sorcerer 1H options and Warlock entirely, and
  half-wires Paladin (in `WeaponUse`, not in `VerifiedClasses`). `LiveGearResolver.DemoteUnusable`
  then demotes a legitimately-equippable weapon as "wrong class". **Fix:** complete the LoH
  per-class matrix and fail-open for unknown combos. *Sev Med-High · Eff M · Conf High*
- [x] **D2 · Maxroll class detection is fragile** _(v0.52.0 — format VERIFIED via the live Rogue/Barbarian builds (both resolve to the correct class), confirming tokens are "ClassName_Skill" with the full class name. So validating the prefix against the 8-class roster is safe: a malformed/empty token now stores null (fail-open) instead of a garbage class string. `NormalizeClass` helper + 4 tests.)_ — derived from `skillBar[0].Split('_')[0]` with
  no validation; empty/odd skill bars yield null/garbage class → all class-aware logic silently
  degrades. **Fix:** read the authoritative planner class id, validate against the 8-class set.
  *Sev Med · Eff S-M · Conf High*
- [ ] **D3 · Weapon assignment isn't class-capacity aware** — `AssignSlots` ignores
  `WeaponSlots(class)` and `SlotPosition`, so Barbarian-arsenal / dual-wield can attach the wrong
  physical weapon to a target slot. **Fix:** thread class capacity + position tie-break.
  *Sev Med · Eff M · Conf Med*
- [x] **D4 · Weapon-type keyword lists are inconsistent** _(VERIFIED NON-BUG — `IsWeaponType` is only applied to `ItemType`/`ItemId` (DiffEngine.cs:400), never to names, so the spurious "blade" keyword can't false-match (no weapon TYPE is "blade"); focus/shield/totem missing from `WeaponKeywords` is harmless because off-hands are slot "offhand" and skip the weapon gate (line 399). The two lists serve different purposes (affinity vs. gate) and needn't match. No functional impact — changing it would be churn.)_ — `WeaponTypeMatch` vs `WeaponKeywords`
  disagree; bare "blade" substring false-matches; focus/shield/totem aren't treated as weapons in
  the off-hand gate. **Fix:** one canonical type table; decide off-hand participation. *Sev Med · Eff S-M · Conf High*
- [ ] **D5 · Multiplier-category & skill-rank priority isn't in scoring.** `IsMultiplier` is parsed
  but feeds only display; `GoalWeights`/category `Pct` weight every affix equally — contra gearing
  doc §5 (uniques/aspects > +ranks > empty multiplier categories > capped > additive). **Fix:**
  add §5 priority weights. *Sev Med · Eff M · Conf High*
- [ ] **D6 · Talisman (Seal/Charm) system has no guidance.** Parsing exists, but no season-pack
  capacity table (Seal rarity → 3/4/5/6 charm slots) and `BuildGuide` notes "no target in schema
  yet" — a whole S13 power system is invisible to %/guidance. **Fix:** add `talismanCapacity` to
  the season pack; route seal/charm targets. *Sev Med · Eff M · Conf High*
- [ ] **D7 · No native in-game loot-filter export** (Apr-2026 feature). The app already computes
  GA counts / wanted affixes / Ancestral / uniques — it just can't emit to the tool players now
  use. **Fix:** add a native-filter serializer alongside D4Companion. *Sev Med · Eff M · Conf High*
- [ ] **D8 · FIND/GET targets aren't Torment-tier gated.** `Activities` emits a generic push line,
  but `BuildGuide` will tell a T1 player to chase T10 Mythics / T2 manuals regardless. **Fix:**
  pass Torment into `BuildGuide.Steps`, gate/annotate against `season_pack.tormentGates`.
  *Sev Med · Eff M · Conf High*
- [~] **D9 · IP-tier handling is slot-blind & the 900 floor is hard-coded.** _(v0.47.0 — the safe half: the 900 floor is now `SeasonPack.AncestralFloorIP` (one place to change for S14, the doc's #1 tripwire). v0.56.0 — added the endgame sub-cap WEAPON advisory: when Torment is captured, an equipped weapon below the IP cap shows a "below the {cap} cap (a {cap} weapon is a flat DPS upgrade)" note (base weapon damage scales with IP). The remaining piece — IP OVERRIDING an affix deficit in upgrade RANKING — stays deferred: it's a judgment-call heuristic that should be validated, not guessed.)_ `UpgradeScorer` weights
  IP uniformly (should be strong on weapons, weak on armor per §5), treats unknown equipped IP as
  not-below-tier, and the 900 floor is duplicated in `Verdicts`/`UpgradeScorer` (not in
  `SeasonPack`) while `Diff.UpgradeItems` doesn't get Torment at all → the two upgrade paths
  disagree. **Fix:** weapon-aware IP credit; move 900 to `SeasonPack`; thread Torment into
  `Diff`. *Sev Med · Eff S-M · Conf High* (S14 IP/Mythic rework lands ~Jun 30 — tripwire)
- [x] **D10 · Umbrella matching over-credits** _(WON'T-CHANGE — VALIDATED against the real build: every "rank/skill" want is a genuine skill rank (Core Skills, Dance of Knives, Imbuement Skills), which "+to All Skills" legitimately covers. The over-credit (All-Skills satisfying a non-skill rank) doesn't manifest on real builds; constraining the umbrella would only risk regressing correct matches. Re-open only if a real build surfaces a non-skill "rank" affix.)_ — "to All Skills" satisfies any want containing
  "rank" (including specific-passive ranks it doesn't cover); bare "Damage" rule is fragile.
  **Fix:** constrain the All-Skills umbrella. *Sev Low-Med · Eff S · Conf Med*
- [ ] **D11 · Tempered affix isn't tracked on the live `Affix`.** Target side has `Tempered`, live
  side doesn't, so the app can't warn "that wrong affix is your temper slot — the Occultist can't
  reroll it." **Fix:** add `Affix.IsTempered` + detect it. *Sev Med · Eff M · Conf Med* ⚑(temper
  tooltip shape)

## Tier E — Hygiene / format-drift hardening

- [ ] **E1 · `samples/sample_tts.log` is Season-8 era** (the designated regression net): every item
  has `Masterwork: N/12` (dead 12-rank system), `Requires Level 60`, IP 770-800 — all tripwire-list
  values. **Fix:** re-stamp to S13 (Quality lines, IP 900, level 70) or promote `sample_tts_s13.log`
  as primary; update the `MasterworkRank==4` assertion. *Sev Med · Eff S*
- [ ] **E2 · `sample_tts_s13.log` still says `Requires Level 60`** (cap is 70). **Fix:** → 70. *Eff S*
- [x] **E3 · "Season 8" comments mislabel** the Quality field (it's the S11 masterwork rework). *Eff S* _(v0.45.0)_
- [ ] **E4 · Class-lock regex** assumes glued `X. Only`; no multi-class ("X or Y Only") / localized
  forms → `ClassLock` silently null. *Eff S-M* ⚑
- [ ] **E5 · en-US number assumption** — `ToNum` strips commas globally; decimal-comma locales
  (German/French D4) corrupt every value 10×/1000×. **Fix:** culture-aware parse or document/gate.
  *Sev Med · Eff M · Conf Med* ⚑(non-English clients)
- [ ] **E6 · Runeword split** is a camel-case heuristic; match the known rune-name set instead. *Eff S*
- [x] **E7 · `NameCandidate`** 64-char ceiling can drop long affixed names. *Eff S* _(v0.50.0 — ceiling
  raised 64→96 with a regression test; the all-caps gate still rejects non-names. The English-only all-caps
  gate is a separate locale concern — still ⚑, with E4/E5.)_
- [x] **E8 · Two diverging `IsTwoHand` helpers** _(VERIFIED INTENTIONAL — they correctly differ:
  `UpgradePath.IsTwoHand` includes bows (bows ARE 2H → ×2 masterwork cost), while
  `DiffEngine.IsTwoHandedType` excludes them because the handedness gate handles ranged via
  `IsRangedWeapon` separately. Merging would break one of the two. No change.)_
- [ ] **E9 · `EndMarkers` ("mouse button"/"action button") is the only way a TTS block closes** in
  the live path — one D4 string-table/localization change, or controller navigation, zeroes out
  capture. **Fix:** close blocks on any strong boundary + a quiet-timeout, keep `EndMarkers` as a
  fast-path. *Sev High · Eff M · Conf Med* ⚑(verify controller/locale wording)

## Verified CORRECT (no action — reassurance)
GA model end-to-end · Infernal Hordes S13 Aether spoils · masterwork Quality 0-25 + capstone ·
uniques-as-rollable data model · Torment captured from char-select & threaded into guidance ·
8-class roster in parser/index/char-select · atomic writes for tombstones & icon PNGs ·
`LogWatcher.Poll` re-entrancy guard + shrink-reset · presence-based scoring · one-enchant-fixable
credit · AspectBlocked on uniques. (14/17 gearing-doc §8 gaps already closed.)

---

## Suggested phasing (each phase ships green per the house ritual)
- **Phase 1 (v0.45.0) — Stop shipping wrong info & data loss:** ✅ SHIPPED — A1, A3, A2, B1, B2, B3,
  B4, B7, C2, C3, E3. (E1/E2 re-stamp the regression-net fixtures, so moved to Phase 4 for focused,
  test-coupled treatment.)
- **Phase 2 (v0.46.0) — UX clarity:** ✅ SHIPPED — C1, C4, C5, C7. (C6 import-feedback already fine;
  C6 icon-progress → Phase 4; C9 deferred for owner sign-off; C11 won't-fix — gate is correct.)
- **Phase 3 (v0.47.0) — Build/class understanding depth:** ✅ SHIPPED (safe subset) — A5 (dedup; the
  "double-count" was a misread), A7 (Mythic all-GA), D9 (900 floor → SeasonPack). A4 won't-change
  (deliberate v0.42 tradeoff). **A6, D1, D2, D4, D10 + D9-behavioral are ⚑ — they change scoring/
  filtering and need real-build / game-data / planner-format verification before implementing, or they
  risk silent regressions in the headline % and gear filtering.** Recommend validating these against the
  real Dance-of-Knives / Whirlwind builds + live log (the §Verification real-data checks).
- **Phase 4 (v0.48.0) — Coverage & hardening:** D3, D5, D6, D7, D8, D11, B5, B6, E1, E2, E4-E9, C6 (icon-progress), C8, C9, C10.
- **⚑ Verification pass:** capture live tooltips for tempered-affix shape (D11/A7), controller/
  locale end-markers (E9), Quality composition, transfiguration marker — fix those after confirming.
